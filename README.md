

## UniProt Bioinformatics Telegram Bot 🧬🤖

این پروژه یک **ربات تلگرام بیوانفورماتیکی** است که امکان **جستجو، نمایش و دانلود اطلاعات پروتئین‌ها** را از پایگاه داده **UniProt** فراهم می‌کند.

---

### 📌 قابلیت‌های اصلی

* جستجوی پروتئین با نام (مثلاً insulin، hemoglobin)
* دریافت اطلاعات پایه پروتئین:

  * نام پروتئین
  * ارگانیسم
  * طول توالی
  * شناسه UniProt
* نمایش توالی پروتئین در تلگرام
* دانلود فایل FASTA
* لینک مستقیم به صفحه UniProt
* استفاده از **Inline Keyboard** برای تعامل بهتر
* ذخیره فایل‌ها و تصاویر کاربران
* پشتیبانی از پیام‌های متنی و تصویری

---

### 🛠️ تکنولوژی‌ها و کتابخانه‌ها

* Python
* `pyTelegramBotAPI (telebot)`
* `requests`
* REST API رسمی UniProt
* `os` و `time` برای مدیریت فایل‌ها

---

### ⚙️ نحوه عملکرد ربات

1. کاربر دستور `/start` را ارسال می‌کند
2. با دستور `/protein <name>`:

   * ابتدا پروتئین‌های از پیش تعریف‌شده بررسی می‌شوند
   * در صورت عدم وجود، درخواست به UniProt REST API ارسال می‌شود
3. اطلاعات پروتئین نمایش داده می‌شود و کاربر می‌تواند:

   * مشاهده لینک UniProt
   * نمایش توالی
   * دانلود فایل FASTA
4. فایل‌ها در پوشه `Data/` ذخیره می‌شوند
5. تعامل با کاربر از طریق دکمه‌های Inline انجام می‌شود

---

### 📂 ساختار پروژه

```
project/
│
├── bot.py
├── Data/
│   ├── <chat_id>/
│   │   └── Pictures/
│   └── <accession>.fasta
└── README.md
```

---

### ▶️ نحوه اجرا

1. نصب وابستگی‌ها:

```bash
pip install pyTelegramBotAPI requests
```

2. قرار دادن توکن ربات:

```python
API_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
```

3. اجرای برنامه:

```bash
python bot.py
```

---

### 🚀 کاربردها

* تمرین بیوانفورماتیک و کار با UniProt API
* آموزش ساخت ربات تلگرام
* مدیریت و دریافت توالی‌های پروتئینی
* پروژه دانشجویی یا نمونه‌کار (Portfolio)

---

### ⚠️ نکات

* توکن ربات را در GitHub منتشر نکنید
* برای درخواست‌های زیاد به UniProt محدودیت API را در نظر بگیرید

---

