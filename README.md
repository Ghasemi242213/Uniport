<!-- برای نمایش بهتر فارسی در GitHub -->

<div dir="rtl" align="right">

# 🤖🧬 ربات تلگرام بیوانفورماتیک UniProt

این پروژه یک **ربات تلگرام بیوانفورماتیکی** است که امکان **جستجو، مشاهده و دانلود اطلاعات پروتئین‌ها** را از پایگاه داده‌ی **UniProt** فراهم می‌کند. این ربات برای اهداف آموزشی، پژوهشی و نمونه‌کار طراحی شده است.

---

## ✨ قابلیت‌ها

* 🔍 جستجوی پروتئین بر اساس نام (مانند: `insulin`، `hemoglobin`)
* 📄 نمایش اطلاعات پایه‌ی پروتئین:

  * نام پروتئین
  * ارگانیسم
  * طول توالی
  * شناسه‌ی UniProt
* 🧬 نمایش توالی پروتئین در تلگرام
* ⬇️ دانلود فایل FASTA
* 🔗 لینک مستقیم به صفحه‌ی UniProt
* 🎛️ استفاده از **Inline Keyboard** برای تعامل بهتر با کاربر
* 💾 ذخیره‌ی فایل‌ها و تصاویر کاربران
* 🖼️ پشتیبانی از پیام‌های متنی و تصویری

---

## 🛠️ تکنولوژی‌ها و کتابخانه‌ها

* Python
* `pyTelegramBotAPI (telebot)`
* `requests`
* UniProt REST API (رسمی)
* `os` و `time` برای مدیریت فایل‌ها

---

## ⚙️ نحوه‌ی عملکرد ربات

1. کاربر دستور `/start` را ارسال می‌کند
2. با دستور `/protein <name>`:

   * ابتدا پروتئین‌های از پیش تعریف‌شده بررسی می‌شوند
   * در صورت عدم وجود، درخواست به UniProt REST API ارسال می‌شود
3. اطلاعات پروتئین نمایش داده می‌شود و کاربر می‌تواند:

   * لینک صفحه‌ی UniProt را مشاهده کند
   * توالی پروتئین را ببیند
   * فایل FASTA را دانلود کند
4. فایل‌ها در پوشه‌ی `Data/` ذخیره می‌شوند
5. تعامل کاربر از طریق دکمه‌های Inline انجام می‌شود

---

## 📂 ساختار پروژه

```text
project/
│
├── bot.py
├── Data/
│   ├── <chat_id>/
│   │   └── Pictures/
│   └── <accession>.fasta
└── README.md
```

---

## ▶️ نحوه‌ی اجرا

### 1️⃣ نصب وابستگی‌ها

```bash
pip install pyTelegramBotAPI requests
```

### 2️⃣ تنظیم توکن ربات

```python
API_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
```

### 3️⃣ اجرای ربات

```bash
python bot.py
```

---

## 🚀 موارد استفاده

* تمرین بیوانفورماتیک و کار با UniProt API
* آموزش ساخت ربات تلگرام با پایتون
* مدیریت و دریافت توالی‌های پروتئینی
* پروژه‌ی دانشجویی یا نمونه‌کار (Portfolio)

---

## ⚠️ نکات مهم

* ❌ توکن ربات را در GitHub منتشر نکنید
* ⏳ در درخواست‌های پرتعداد، محدودیت UniProt API را در نظر بگیرید

---
